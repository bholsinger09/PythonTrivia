# API Endpoints Added

## Session Management Endpoints

Added /api/me and /api/logout endpoints for proper session management:

- `/api/me` - GET endpoint to check current user session status
- `/api/logout` - POST endpoint to clear session and logout user

These endpoints support the static authentication system.