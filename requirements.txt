# Python Trivia App - Minimal Dependencies
# Only the essential packages needed for the clean Flask application

# Core Framework
Flask==2.3.3

# WSGI Server for Deployment
gunicorn==23.0.0

# Template Engine (included with Flask, but specified for clarity)
Jinja2==3.1.6

# Security utilities (included with Flask)
MarkupSafe==3.0.3
itsdangerous==2.2.0
Werkzeug==3.1.3

# Click for CLI support (Flask dependency)
click==8.3.0
blinker==1.9.0

# Optional: Environment variable support
python-dotenv==1.0.0